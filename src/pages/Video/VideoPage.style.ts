import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  grid: {
    display: 'grid',
    placeItems: 'center',
    height: '100vh',
    margin: '0',
  },
  container: {
    textAlign: 'center',
  },
  text: {
    width: '50%',
  },
}));
